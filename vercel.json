{
  "buildCommand": "cd exampleSite && mkdir -p themes && ln -sf ../../ themes/hugo-profile && hugo --config hugo.yaml --gc --minify",
  "outputDirectory": "exampleSite/public",
  "framework": "hugo"
}
