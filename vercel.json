{
  "buildCommand": "mkdir -p exampleSite/themes && ln -sf $PWD exampleSite/themes/hugo-profile && cd exampleSite && hugo --config hugo.yaml --gc --minify",
  "outputDirectory": "exampleSite/public",
  "framework": "hugo"
}
